export interface IProductType {
  companies_id: number;
  description: string;
  id: string;
  name: string;
  slug: string;
  uuid: string;
  weight: number;
};

export interface IFile {
  field_name: string;
  id: string;
  name: string;
};